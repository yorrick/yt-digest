slack:
  webhook_url: ${SLACK_WEBHOOK_URL}

openrouter:
  model: deepseek/deepseek-v4.1-flash
  provider: deepinfra
  timeout: 90

apify:
  timeout: 120
  max_charge_usd: 0.05

db_path: ~/.yt-digest/data.db
